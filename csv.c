#include <zlib.h>
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
   if (argc==2) {
        char* filename = argv[1];
   }else{
        printf("usage: %s [file_csvz]",argv[0]);
        return 0;
   }


}
struct sums{
};
sumcsv(struct sums *totals){


}
